export default {
  Lhedy: process.env.NAME, // actual name of the recipient (Mandatory)

  Babe: process.env.NICKNAME, // nickname(optional)

  pic: process.env.PIC, // image url of recipients (Mandatory)

  showScrollMsg: process.env.SCROLL_MSG, // set to false if you do not want the scrolling message

  2004/07/07: process.env.BIRTH_DATE, // Mention birthday date in YYYY-MM-DD format
};
